#pragma once

#include "menu.hpp"

namespace Mine {
	extern const MenuItem MAIN; //extern для того, чтобы объявить переменную, но определить её не здесь, а в cpp
	
	extern const MenuItem READ;
	extern const MenuItem EXIT;
	
	extern const MenuItem PUSHKIN;
	extern const MenuItem LERMONTOV;
	extern const MenuItem KRYLOV;
	extern const MenuItem MAIN_MENU;
	
	extern const MenuItem DUBROVSKY;
	extern const MenuItem EVGENY_ONEGIN;
	extern const MenuItem ZIMNY_VECHER;
	extern const MenuItem PREVIOUS_MENU;
}